#ifndef POINTCLOUD
#define POINTCLOUD
int stat1();
int main();

struct point{		//this struct is used to keep track of points in a condensed and easy manner
	double height, x, y;
};
#endif // !POINTCLOUD

